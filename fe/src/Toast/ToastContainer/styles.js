import styled from 'styled-components';

export const Container = styled.div`
  position: fixed;
  z-index: 2;
  bottom: 40px;
  left: 50%;
  transform: translateX(-50%);

  img {
    margin-right: 8px;
  }
`;
